# Railway — نسخة تجريبية
# وجود Dockerfile يجعل Railway يبني منه تلقائيًا

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node server/app.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
