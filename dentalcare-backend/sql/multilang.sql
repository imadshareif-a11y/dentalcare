-- multilang.sql
-- -----------------------------------------------------------
-- ملاحظة تصميم مهمة: أسماء المرضى (parties.name) ما بتتغيّر —
-- عمود واحد بس، بدون ترجمة، لحماية دقة تعريف هوية المريض.
-- فقط الكيانات "المعرّفة مسبقًا والمحدودة العدد" (شجرة الحسابات،
-- كتالوج العلاجات لاحقًا) بتاخد أعمدة ترجمة، لأنها بتترجم مرة
-- وحدة يدويًا من قبل مدير العيادة، مش آليًا.
-- -----------------------------------------------------------

-- 1) لغة تفضيلية لكل مستخدم — الواجهة بتفتح بيها تلقائيًا
ALTER TABLE users
    ADD COLUMN locale VARCHAR(5) NOT NULL DEFAULT 'ar'
    CHECK (locale IN ('ar', 'en', 'he'));

-- 2) ترجمة أسماء الحسابات — 3 أعمدة صريحة بدل جدول ترجمة عام،
-- عشان تبقى بسيطة تُدار وتُفهم (3 لغات بس، عدد حسابات محدود)
ALTER TABLE chart_of_accounts
    ADD COLUMN account_name_ar VARCHAR(255),
    ADD COLUMN account_name_en VARCHAR(255),
    ADD COLUMN account_name_he VARCHAR(255);

-- ننقل القيم الحالية من account_name (العربي) للعمود الجديد،
-- كخطوة انتقالية بلا فقدان بيانات
UPDATE chart_of_accounts SET account_name_ar = account_name WHERE account_name_ar IS NULL;

-- English وHebrew تضل فاضية لحد ما مدير العيادة يعبّيها يدويًا
-- من شاشة الإعدادات — الواجهة بترجع للعربي تلقائيًا لو الترجمة
-- المطلوبة مش موجودة بعد (fallback chain)
